[project]
name = "manga-trace-web-mvp"
version = "0.1.0"
description = "Minimal FastAPI MVP for per-view forensic-watermarked manga image delivery"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.ruff]
line-length = 100
